name: Push to main

on:
  push:
    branches:
      - test-release-tags
    tags:
      - '**'
env:
  QUAY_ORG: quay.io/rh_ee_camadorg
  QUAY_CHARTS: quay.io/rh_ee_camadorg
  QUAY_STANDALONE_REPO: flightctl-ui
  QUAY_OCP_REPO: flightctl-ocp-ui

jobs:
  publish-flightctl-ui:
    if: github.repository == 'skip-this-job'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Generate tags
        id: generate-tags
        run: |
          if ${{ github.ref_type == 'tag' }}; then
            tags=( ${{ github.ref_name }} )
            echo "tags=${tags[@]}" >> $GITHUB_OUTPUT
            echo "tags=${tags[@]}"
          else
            tags=( latest-${GITHUB_SHA} latest )
            echo "tags=${tags[@]}" >> $GITHUB_OUTPUT
            echo "tags=${tags[@]}"
          fi

      - name: Build
        id: build
        uses: redhat-actions/buildah-build@v2
        with:
          image: ${{ env.QUAY_STANDALONE_REPO }}
          tags: ${{ steps.generate-tags.outputs.tags }}
          labels: |
            org.flightctl.flightctl-ui.github.repository=${{ github.repository }}
            org.flightctl.flightctl-ui.github.actor=${{ github.actor }}
            org.flightctl.flightctl-ui.github.run_id=${{ github.run_id }}
            org.flightctl.flightctl-ui.github.sha=${{ github.sha }}
            org.flightctl.flightctl-ui.github.ref_name=${{ github.ref_name }}
          extra-args: |
            --ulimit nofile=10000:10000
          containerfiles: Containerfile
          context: .

      - name: Push to Quay.io
        id: push
        uses: redhat-actions/push-to-registry@v2.7
        with:
          image: ${{ steps.build.outputs.image }}
          tags: ${{ steps.build.outputs.tags }}
          registry: ${{ env.QUAY_ORG }}
          username: ${{ secrets.QUAY_CAMADOR_INFRA_ROBOT_USERNAME }}
          password: ${{ secrets.QUAY_CAMADOR_INFRA_ROBOT_PASSWORD }}

  publish-flightctl-ocp-ui:
    if: github.repository == 'skip-this-job'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Generate tags
        id: generate-tags
        run: |
          if ${{ github.ref_type == 'tag' }}; then
            tags=( ${{ github.ref_name }} )
            echo "tags=${tags[@]}" >> $GITHUB_OUTPUT
            echo "tags=${tags[@]}"
          else
            tags=( latest-${GITHUB_SHA} latest )
            echo "tags=${tags[@]}" >> $GITHUB_OUTPUT
            echo "tags=${tags[@]}"
          fi

      - name: Build
        id: build
        uses: redhat-actions/buildah-build@v2
        with:
          image: ${{ env.QUAY_OCP_REPO }}
          tags: ${{ steps.generate-tags.outputs.tags }}
          labels: |
            org.flightctl.flightctl-ui.github.repository=${{ github.repository }}
            org.flightctl.flightctl-ui.github.actor=${{ github.actor }}
            org.flightctl.flightctl-ui.github.run_id=${{ github.run_id }}
            org.flightctl.flightctl-ui.github.sha=${{ github.sha }}
            org.flightctl.flightctl-ui.github.ref_name=${{ github.ref_name }}
          extra-args: |
            --ulimit nofile=10000:10000
          containerfiles: Containerfile.ocp
          context: .

      - name: Push to Quay.io
        id: push
        uses: redhat-actions/push-to-registry@v2.7
        with:
          image: ${{ steps.build.outputs.image }}
          tags: ${{ steps.build.outputs.tags }}
          registry: ${{ env.QUAY_ORG }}
          username: ${{ secrets.QUAY_CAMADOR_INFRA_ROBOT_USERNAME }}
          password: ${{ secrets.QUAY_CAMADOR_INFRA_ROBOT_PASSWORD }}

  publish-helm-charts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Get version
        run: |          
          VERSION=$(git describe --long --tags --always)
          echo "VERSION1=${VERSION}"
          VERSION=${VERSION#v} # remove the leading v prefix for version
          echo "VERSION=${VERSION}" >> $GITHUB_ENV
          echo "VERSION=${VERSION}"

      - name: Build helm charts
        run: |
          echo packaging "${VERSION}"
          helm package ./apps/standalone/deploy/helm/flightctl-ui --version "${VERSION}" --app-version "${VERSION}"
          helm package ./apps/ocp-plugin/deploy/helm/flightctl-ocp-ui --version "${VERSION}" --app-version "${VERSION}"

      - name: Login helm
        env:
          PASSWORD: ${{ secrets.QUAY_CAMADOR_INFRA_ROBOT_PASSWORD }}
          USER: ${{ secrets.QUAY_CAMADOR_INFRA_ROBOT_USERNAME }}
        run: helm registry login quay.io -u ${USER} -p ${PASSWORD}

      - name: Push helm charts
        run: |
          helm push flightctl-charts-ui-*.tgz oci://${{ env.QUAY_CHARTS }}/
          helm push flightctl-charts-ocp-ui-*.tgz oci://${{ env.QUAY_CHARTS }}/
